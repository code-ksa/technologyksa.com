# دليل البدء السريع - نظام CMS

## 🎯 الخطوات الأساسية (3 خطوات فقط!)

### الخطوة 1: إضافة المحتوى
1. افتح `/admin/index.html` في المتصفح
2. أضف صفحات/مقالات/خدمات/مشاريع كما تريد
3. احفظ كل شيء

### الخطوة 2: تصدير البيانات
1. اضغط على زر **"تصدير البيانات"** الأخضر في أعلى لوحة التحكم
2. اضغط **"تحميل data.json"**
3. انسخ الملف المحمّل إلى المجلد الرئيسي للمشروع (استبدل الملف القديم)

### الخطوة 3: بناء الموقع
```bash
node build-improved.js
```

**🎉 انتهى! جميع صفحاتك ومحتواك أصبح جاهزاً في مجلد `dist/`**

---

## 📝 تفاصيل إضافية

### إدارة الصفحات

#### إنشاء صفحة جديدة
1. اذهب إلى **الصفحات** من القائمة الجانبية
2. اضغط **"إضافة صفحة جديدة"**
3. املأ المعلومات
4. احفظ

#### استخدام Page Builder
1. بعد إنشاء الصفحة، اذهب إلى **Page Builder**
2. اضغط **"اختيار صفحة"** واختر الصفحة التي تريد تصميمها
3. اسحب العناصر من الجانب وأفلتها في المنتصف
4. اضغط **"حفظ التخطيط"**
5. اضغط **"نشر الصفحة"**

### إدارة المقالات

1. اذهب إلى **المدونة**
2. اضغط **"إضافة مقال جديد"**
3. املأ المعلومات
4. اختر الحالة: **منشور** أو **مسودة**
5. احفظ

### إدارة الخدمات والمشاريع

نفس الطريقة، لكل قسم صفحته الخاصة في القائمة الجانبية.

### تخصيص الموقع

#### القوائم (Menus)
1. اذهب إلى **القوائم**
2. عدّل القائمة الرئيسية أو قائمة Footer
3. أضف/احذف/رتب العناصر
4. احفظ

#### إعدادات الموقع
1. اذهب إلى **الإعدادات**
2. عدّل:
   - معلومات الموقع
   - Header/Footer (الألوان، الأزرار)
   - معلومات التواصل
   - روابط السوشيال ميديا
3. احفظ الإعدادات

---

## ⚠️ مهم جداً!

### يجب تصدير البيانات بعد كل تعديل

لوحة التحكم تحفظ البيانات في **localStorage** (ذاكرة المتصفح).
نظام البناء يقرأ من **data.json** (ملف خارجي).

**لذلك:**
- بعد كل إضافة/تعديل/حذف
- اضغط زر **"تصدير البيانات"**
- احفظ الملف data.json
- شغّل البناء

### زر "تصدير البيانات"

الزر **الأخضر** في أعلى لوحة التحكم:
```
┌─────────────────────────────────────────┐
│  📥 تصدير البيانات  │  ➕ إضافة ...  │
└─────────────────────────────────────────┘
```

عند الضغط عليه:
1. ستظهر نافذة توضح:
   - عدد المقالات والصفحات والخدمات المُصدّرة
   - الخطوات التالية
2. اضغط **"تحميل data.json"**
3. انسخ الملف إلى مجلد المشروع

---

## 🔍 حل المشاكل

### المشكلة: المحتوى لا يظهر بعد البناء

**السبب:** لم يتم تصدير البيانات

**الحل:**
1. تأكد من تصدير data.json
2. تأكد من نسخ الملف إلى المجلد الصحيح
3. شغّل البناء مرة أخرى

### المشكلة: صور Assets لا تظهر (404)

**السبب:** تم إصلاح هذه المشكلة! المسارات الآن نسبية وصحيحة.

**إذا استمرت المشكلة:**
1. تأكد من وجود المجلد `assets/` في المشروع
2. شغّل البناء مرة أخرى (سيتم نسخ Assets تلقائياً)

### المشكلة: Page Builder لا يعمل

**الحل:**
1. تأكد من إنشاء صفحة أولاً من قسم "الصفحات"
2. في Page Builder، اضغط "اختيار صفحة"
3. اختر الصفحة من القائمة
4. الآن يمكنك إضافة العناصر

---

## 📊 نصائح للأداء الأفضل

### 1. النسخ الاحتياطي
احتفظ بنسخة احتياطية من `data.json` بشكل دوري!

### 2. التسميات
استخدم أسماء واضحة للصفحات والمقالات:
- ✅ `about-us` (جيد)
- ❌ `page 1` (سيء)
- ❌ `صفحة عنا` (سيء - استخدم الإنجليزية للـ slugs)

### 3. الصور
استخدم صور بجودة معقولة (لا تكون كبيرة جداً):
- الحجم المثالي: أقل من 500 KB
- الأبعاد: 1200x800 للصور الكبيرة

### 4. التحديثات المتكررة
إذا كنت تعدّل محتوى كثيراً:
1. أضف/عدّل كل المحتوى دفعة واحدة
2. صدّر البيانات مرة واحدة
3. ابنِ الموقع مرة واحدة

---

## 🚀 البدء الآن!

1. افتح `/admin/index.html`
2. أضف محتوى
3. صدّر البيانات
4. شغّل البناء
5. استمتع بموقعك! 🎉

---

## 📞 المساعدة

إذا واجهت أي مشكلة:
1. راجع قسم "حل المشاكل" أعلاه
2. تحقق من Console المتصفح (F12) للأخطاء
3. تأكد من اتباع الخطوات بالترتيب

**العملية بسيطة:** إضافة → تصدير → بناء ✨

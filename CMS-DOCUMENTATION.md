# نظام إدارة المحتوى - Technology KSA

## 📋 نظرة عامة

نظام إدارة محتوى شامل ومتكامل يتيح التحكم الكامل في جميع عناصر الموقع من خلال لوحة تحكم واحدة.

---

## 🎯 المميزات الرئيسية

### 1. **إدارة الصفحات (Pages Management)**

#### إنشاء صفحة جديدة:
1. اذهب إلى "الصفحات" في لوحة التحكم
2. اضغط "إضافة صفحة جديدة"
3. أدخل:
   - عنوان الصفحة
   - Slug (يتم إنشاؤه تلقائياً)
   - Meta Title & Description
   - الحالة (منشورة/مسودة)
4. احفظ الصفحة

#### Quick Actions المتوفرة:
- **تعديل (Edit)**: تعديل معلومات الصفحة
- **Page Builder**: فتح محرر الصفحة
- **تكرار (Duplicate)**: نسخ الصفحة بالكامل
- **إضافة للقائمة**: إضافة الصفحة مباشرة لقائمة معينة
- **حذف (Delete)**: حذف الصفحة

---

### 2. **Page Builder - منشئ الصفحات**

#### إضافة عناصر:
**الطريقة الأولى - Drag & Drop:**
- اسحب العنصر من الشريط الجانبي
- أفلته في منطقة العمل

**الطريقة الثانية - أزرار سريعة:**
- اضغط على الأزرار في أعلى الصفحة:
  - `+ Hero`
  - `+ CTA`
  - `+ Features`
  - `+ Testimonials`
  - `+ Stats`

#### أنواع العناصر المتاحة:
1. **Hero Section** - قسم رئيسي مع صورة خلفية
2. **CTA Section** - دعوة لاتخاذ إجراء
3. **Features** - عرض الميزات (مع أيقونات)
4. **Testimonials** - آراء العملاء
5. **Statistics** - إحصائيات وأرقام
6. **Contact Form** - نموذج اتصال
7. **Team Members** - عرض الفريق
8. **Pricing Tables** - جداول الأسعار

#### التحكم في العناصر:
- **↑ نقل لأعلى**: تغيير ترتيب العنصر
- **↓ نقل لأسفل**: تغيير ترتيب العنصر
- **📋 تكرار**: نسخ العنصر
- **✏️ تعديل**: فتح محرر العنصر
- **🗑️ حذف**: حذف العنصر

#### تعديل العنصر:
1. اضغط "تعديل" على العنصر
2. قم بتعديل:
   - **Hero**: العنوان، النص، زر، صورة الخلفية، اللون
   - **CTA**: العنوان، الوصف، نص الزر، الرابط
   - **Features**: أضف/عدل ميزات متعددة (أيقونة، عنوان، وصف)
   - **Testimonials**: أضف/عدل شهادات (اسم، منصب، شركة، صورة، تقييم، نص)
   - **Statistics**: أضف/عدل إحصائيات (أيقونة، رقم، نص)
3. احفظ التغييرات

---

### 3. **نظام القوائم (Menus Management)**

#### إنشاء قائمة جديدة:
1. اذهب إلى "القوائم"
2. اضغط "إضافة قائمة جديدة"
3. أدخل:
   - اسم القائمة
   - الموقع (Header/Footer-1/Footer-2/Footer-3/Sidebar)
4. اختر الصفحات المتاحة (checkboxes)
5. أو أضف روابط مخصصة
6. رتب العناصر
7. احفظ القائمة

#### الأزرار المتاحة في كل عنصر:
- **✏️ تعديل الصفحة**: فتح محرر الصفحة المرتبطة
- **🔨 Page Builder**: فتح Page Builder للصفحة
- **✏️ تعديل العنصر**: تعديل عنوان/رابط العنصر
- **🗑️ حذف**: إزالة من القائمة

#### إضافة صفحة جديدة من داخل القائمة:
- اضغط "إضافة صفحة جديدة" في أعلى قسم الصفحات
- أدخل عنوان الصفحة
- يتم إضافتها تلقائياً للقائمة

---

### 4. **إعدادات الموقع (Settings)**

#### الإعدادات الأساسية:
- اسم الموقع (عربي/إنجليزي)
- الوصف
- اللوجو (3 أنواع: عادي، Light، Dark)
- Favicon

#### إعدادات الهيدر:
- **الثيم**: Light / Dark / Auto
- **الموقع**: Static / Fixed / Sticky
- **إظهار البحث**: نعم/لا
- **إظهار مبدل اللغة**: نعم/لا
- **زر CTA**: النص والرابط

#### إعدادات الفوتر:
- **التخطيط**: 2 / 3 / 4 أعمدة
- **إظهار CTA**: نعم/لا
- **قوائم الأعمدة**: اختر قائمة لكل عمود

#### معلومات التواصل:
- رقم الهاتف
- البريد الإلكتروني
- رقم الواتساب
- العنوان

#### Social Media:
- Facebook
- Twitter
- LinkedIn
- Instagram
- YouTube

---

## 💾 نظام التخزين (localStorage)

### البيانات المخزنة:

```javascript
// الصفحات
localStorage.getItem('techksa_pages')

// القوائم
localStorage.getItem('techksa_menus')

// الإعدادات
localStorage.getItem('techksa_site_settings')

// المقالات
localStorage.getItem('techksa_blog_posts')

// الخدمات
localStorage.getItem('techksa_services')

// المشاريع
localStorage.getItem('techksa_projects')

// مكتبة الصور
localStorage.getItem('techksa_media')

// العناصر (Components)
localStorage.getItem('techksa_components_hero')
localStorage.getItem('techksa_components_cta')
// ... etc
```

---

## 🔧 Site Loader - الربط الديناميكي

### كيفية ربط صفحة جديدة:

#### 1. إضافة Script في نهاية الصفحة:

```html
<!-- قبل </body> -->
<script src="assets/js/site-loader.js"></script>
```

#### 2. تحديث Header Structure:

استبدل الـ Header الثابت بـ:

```html
<header class="site-header" id="siteHeader">
  <!-- سيتم ملؤه تلقائياً من site-loader.js -->
</header>
```

#### 3. تحديث Footer Structure:

استبدل الـ Footer الثابت بـ:

```html
<footer class="site-footer" id="siteFooter">
  <!-- سيتم ملؤه تلقائياً من site-loader.js -->
</footer>
```

### ما يقوم به Site Loader تلقائياً:

✅ **يحمّل الإعدادات** من localStorage
✅ **يحمّل القوائم** من localStorage
✅ **يبني الـ Header** مع القائمة الصحيحة
✅ **يبني الـ Footer** مع القوائم المحددة
✅ **يطبق الثيم** (Light/Dark/Auto)
✅ **يطبق موقع الهيدر** (Static/Fixed/Sticky)
✅ **يضيف روابط Social Media**
✅ **يُفعّل Mobile Menu**
✅ **يُحدّث الروابط** ويميّز الصفحة الحالية

### استخدام Site Loader في الكود:

```javascript
// الوصول إلى الإعدادات
const siteName = siteLoader.getSetting('siteName');
const phone = siteLoader.getSetting('phone');

// الوصول إلى قائمة معينة
const headerMenu = siteLoader.getMenu('header');
const footerMenu1 = siteLoader.getMenu('footer-1');

// الوصول إلى صفحة
const page = siteLoader.getPage('about-us');
```

---

## 📝 سير العمل الموصى به

### لإضافة صفحة جديدة كاملة:

1. **إنشاء الصفحة**:
   ```
   Pages → إضافة صفحة جديدة
   ```

2. **تصميم الصفحة**:
   ```
   افتح Page Builder
   أضف العناصر المطلوبة
   عدّل كل عنصر
   احفظ التخطيط
   ```

3. **إضافة للقائمة**:
   ```
   Pages → اضغط زر "إضافة للقائمة"
   أو
   Menus → افتح القائمة → اختر الصفحة
   ```

4. **نشر الصفحة**:
   ```
   غيّر الحالة إلى "منشورة"
   ```

### لتعديل قائمة موجودة:

1. **فتح القائمة**:
   ```
   Menus → تعديل القائمة المطلوبة
   ```

2. **تعديل العناصر**:
   - أضف صفحات جديدة
   - أضف روابط مخصصة
   - عدّل عناصر موجودة
   - رتب العناصر

3. **حفظ**:
   ```
   احفظ التغييرات
   ```

---

## 🎨 Styling & Customization

### تخصيص الألوان:
انتقل إلى `Settings` وعدّل:
- لون الهيدر
- لون الفوتر
- استخدم Color Picker

### تخصيص الشعار:
رفّع 3 أنواع من الشعارات:
- **Logo العادي**: للاستخدام العام
- **Logo Light**: للخلفيات الداكنة
- **Logo Dark**: للخلفيات الفاتحة

---

## 🔗 الروابط والـ URLs

### صيغة الروابط الموصى بها:

```
الصفحات: slug.html
المقالات: blog/post-slug.html
الخدمات: services/service-slug.html
المشاريع: portfolio/project-slug.html
```

### الروابط الداخلية:
```html
<a href="#section-id">رابط لقسم في نفس الصفحة</a>
<a href="page.html">رابط لصفحة أخرى</a>
<a href="page.html#section">رابط لقسم في صفحة أخرى</a>
```

---

## ⚠️ نصائح مهمة

1. **احفظ دائماً** قبل الانتقال لصفحة أخرى
2. **اختبر الصفحة** في Page Builder قبل نشرها
3. **لا تحذف** الصفحة الرئيسية (Home)
4. **استخدم slug واضح** لكل صفحة
5. **املأ Meta Title & Description** لكل صفحة (SEO)
6. **رتّب العناصر** بشكل منطقي في Page Builder
7. **راجع القوائم** بعد إضافة صفحات جديدة

---

## 🐛 حل المشاكل الشائعة

### المشكلة: القائمة لا تظهر
**الحل**: تأكد من:
- حفظ القائمة
- تحديد الموقع الصحيح (Header/Footer)
- وجود عناصر في القائمة

### المشكلة: التغييرات لا تظهر
**الحل**:
- امسح الـ Cache
- تأكد من حفظ الإعدادات
- أعد تحميل الصفحة

### المشكلة: Page Builder فارغ
**الحل**:
- تأكد من اختيار صفحة
- استخدم الأزرار السريعة لإضافة عناصر

### المشكلة: الروابط لا تعمل
**الحل**:
- تأكد من صحة الـ Slug
- تأكد من وجود .html في النهاية
- راجع الروابط في القائمة

---

## 📱 التوافق

✅ **الأجهزة المدعومة:**
- Desktop (جميع المقاسات)
- Tablet (iPad, Android tablets)
- Mobile (جميع الأحجام)

✅ **المتصفحات المدعومة:**
- Chrome (آخر إصدارين)
- Firefox (آخر إصدارين)
- Safari (آخر إصدارين)
- Edge (آخر إصدارين)

✅ **اللغات:**
- العربية (RTL) ✓
- الإنجليزية (LTR) - قريباً

---

## 🚀 التحديثات القادمة

- [ ] Multi-language Support كامل
- [ ] Page Templates جاهزة
- [ ] Import/Export للصفحات
- [ ] Version Control للصفحات
- [ ] Media Library محسّنة
- [ ] SEO Analyzer مدمج
- [ ] Performance Optimizer
- [ ] Backup/Restore System

---

## 📞 الدعم

للدعم التقني أو الاستفسارات:
- **البريد الإلكتروني**: support@technologyksa.com
- **الهاتف**: +966 50 123 4567

---

**تم التطوير بواسطة**: Technology KSA
**الإصدار**: 2.0.0
**آخر تحديث**: 2025-01-10

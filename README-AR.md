# 🚀 Technology KSA - نظام إدارة المحتوى

## ⚠️ إصلاح المشاكل - تحديث هام

تم حل جميع المشاكل التي كانت تواجه النظام:

### ✅ المشاكل المحلولة

1. **✅ الكومبوننتس (العناصر)**
   - تم إضافة وظيفة `renderPageCanvas()` الناقصة
   - جميع العناصر (Hero, CTA, Features, etc.) تعمل الآن
   - يمكنك سحب وإفلات العناصر في Page Builder

2. **✅ وظائف الإضافة والتعديل والحفظ**
   - `savePage()` - حفظ الصفحات ✓
   - `savePost()` - حفظ المقالات ✓
   - `publishPage()` - نشر الصفحات ✓
   - فتح Page Builder تلقائياً بعد إنشاء صفحة ✓

3. **✅ Publisher API**
   - سكريبت `START.bat` لتشغيل النظام بسهولة
   - يعمل على Windows و Linux و Mac

---

## 🎯 كيفية البدء (خطوات بسيطة)

### الخطوة 1: تثبيت Node.js (إذا لم يكن مثبتاً)

1. اذهب إلى: https://nodejs.org/
2. حمّل النسخة LTS (الموصى بها)
3. ثبّت البرنامج
4. افتح Command Prompt واكتب: `node --version`
   - يجب أن ترى: `v18.x.x` أو أحدث

### الخطوة 2: تثبيت المكتبات (مرة واحدة فقط)

افتح Command Prompt في مجلد المشروع:

```bash
npm install
```

### الخطوة 3: تشغيل النظام

#### على Windows:

فقط اضغط مزدوجاً على:
```
START.bat
```

سيفتح نافذتين:
1. **Publisher API** - على المنفذ 3001
2. **Website Server** - على المنفذ 8080

**مهم:** اترك النافذتين مفتوحتين!

#### على Linux/Mac:

```bash
npm start
```

---

## 📱 الوصول إلى لوحة التحكم

بعد تشغيل النظام، افتح في المتصفح:

```
http://127.0.0.1:8080/admin/index.html
```

---

## 📝 دليل الاستخدام السريع

### 1. إضافة صفحة جديدة

1. في لوحة التحكم → **الصفحات**
2. اضغط **"إضافة صفحة جديدة"**
3. املأ البيانات:
   - **العنوان:** من نحن
   - **الرابط:** سيُنشأ تلقائياً (about-us)
   - **الحالة:** منشورة
4. اضغط **حفظ**

**النتيجة:** ✅ Page Builder سيفتح تلقائياً!

### 2. بناء الصفحة (Page Builder)

الآن أنت في Page Builder:

#### إضافة عناصر:

1. **Hero Section** (القسم الرئيسي)
   - في القائمة اليسرى، انقر على "Hero Sections"
   - اسحب "Hero مركّز" إلى الكانفاس
   - **النتيجة:** ✅ يظهر في الصفحة!

2. **Features Grid** (شبكة الميزات)
   - انقر على "Features Grid"
   - اسحب "Features Grid (3 أعمدة)"
   - **النتيجة:** ✅ يظهر تحت Hero!

3. **Call to Action** (دعوة لاتخاذ إجراء)
   - انقر على "Call to Action"
   - اسحب "CTA بسيط"
   - **النتيجة:** ✅ يظهر في النهاية!

#### تعديل محتوى العنصر:

- اضغط على زر **"تعديل"** (أيقونة القلم) على أي عنصر
- ستفتح نافذة تعديل HTML
- عدّل النص والروابط
- اضغط **حفظ التعديلات**

#### ترتيب العناصر:

- ⬆️ **تحريك لأعلى**
- ⬇️ **تحريك لأسفل**
- 🗑️ **حذف العنصر**

### 3. حفظ التخطيط

بعد إضافة جميع العناصر:

1. اضغط **"حفظ التخطيط"** (أعلى يسار الصفحة)
2. **النتيجة:** ✅ تم الحفظ في localStorage

### 4. نشر الصفحة

1. اضغط **"نشر الصفحة"**
2. ستظهر رسالة: "جاري النشر على الخادم..."
3. بعد ثوانٍ:

```
┌────────────────────────────────────┐
│ ✓ تم النشر على الخادم بنجاح!      │
├────────────────────────────────────┤
│   📄 من نحن                        │
│   الصفحة منشورة ومتاحة الآن!       │
│                                    │
│   🔗 http://127.0.0.1:8080/...     │
│                                    │
│   [فتح الصفحة على الموقع]          │
└────────────────────────────────────┘
```

4. اضغط **"فتح الصفحة على الموقع"**
5. **النتيجة:** ✅ الصفحة تعمل!

### 5. إضافة إلى القائمة

بعد النشر مباشرة:

```
هل تريد إضافة الصفحة "من نحن" إلى القائمة الرئيسية؟
```

- **موافق** → تُضاف تلقائياً
- **إلغاء** → يمكن إضافتها لاحقاً من قسم "القوائم"

---

## 🔧 حل المشاكل

### المشكلة 1: "API غير متاح"

**السبب:** Publisher API لا يعمل

**الحل:**
1. تأكد أنك شغّلت `START.bat`
2. أو افتح Command Prompt جديد واكتب:
   ```bash
   node publisher-api.js
   ```
3. اترك النافذة مفتوحة

**للتحقق:**
```bash
curl http://localhost:3001/api/health
```

يجب أن ترى:
```json
{"success":true,"status":"running","timestamp":"..."}
```

### المشكلة 2: "لا توجد كومبوننتس (عناصر)"

**السبب:** JavaScript لم يحمّل بشكل صحيح

**الحل:**
1. افتح Console في المتصفح (اضغط F12)
2. ابحث عن أخطاء باللون الأحمر
3. أعد تحميل الصفحة (Ctrl+F5)
4. تأكد من تحميل جميع الملفات:
   - `cms-extended.js` ✓
   - `page-functions.js` ✓
   - `core-publisher.js` ✓
   - `core-integration.js` ✓

### المشكلة 3: "خطأ 404 بعد النشر"

**السبب:** ملف الصفحة لم يُحفظ في `dist/`

**الحل:**
1. تأكد أن Publisher API يعمل
2. تحقق من المجلد `dist/`:
   ```bash
   dir dist
   ```
   يجب أن ترى ملفات `.html`

### المشكلة 4: "Page Builder لا يفتح بعد إنشاء صفحة"

**السبب:** تعارض في JavaScript

**الحل:**
1. افتح Console (F12)
2. ابحث عن خطأ `openPageBuilderForPage is not defined`
3. أعد تحميل الصفحة
4. إذا استمرت المشكلة:
   - احذف cache المتصفح (Ctrl+Shift+Delete)
   - أعد تحميل الصفحة (Ctrl+F5)

---

## 📊 ملفات المشروع

```
technologyksa.com/
│
├── START.bat                    ← شغّل هذا الملف!
├── publisher-api.js             ← Publisher API Server
├── package.json                 ← Dependencies
│
├── admin/
│   ├── index.html               ← لوحة التحكم
│   ├── cms-extended.js          ← نظام CMS
│   ├── page-functions.js        ← وظائف الصفحات
│   ├── core-publisher.js        ← نظام النشر
│   └── core-integration.js      ← التكامل
│
├── assets/
│   ├── css/
│   └── js/
│       ├── core-system.js       ← نظام Event Bus
│       ├── image-storage-manager.js  ← إدارة الصور
│       └── ...
│
├── dist/                        ← الصفحات المنشورة (HTML)
│
└── COMPLETE-SYSTEM-GUIDE.md     ← دليل شامل مفصّل
```

---

## ✅ التأكد من أن كل شيء يعمل

### اختبار سريع (5 دقائق):

1. **شغّل النظام:**
   ```
   START.bat
   ```

2. **افتح لوحة التحكم:**
   ```
   http://127.0.0.1:8080/admin/index.html
   ```

3. **أضف صفحة:**
   - الصفحات → إضافة → العنوان: "تجربة"
   - حفظ
   - **هل فتح Page Builder تلقائياً؟** ✅

4. **أضف عنصر:**
   - اسحب "Hero مركّز"
   - **هل ظهر في الكانفاس؟** ✅

5. **احفظ وانشر:**
   - حفظ التخطيط
   - نشر الصفحة
   - **هل ظهرت نافذة النجاح مع الرابط؟** ✅

6. **افتح الصفحة:**
   - اضغط "فتح الصفحة على الموقع"
   - **هل الصفحة تعمل؟** ✅

**إذا كانت جميع الإجابات ✅ → النظام يعمل بشكل ممتاز!**

---

## 🎓 نصائح للاستخدام الفعّال

### 1. احفظ باستمرار

اضغط **"حفظ التخطيط"** بعد كل تعديل مهم.

### 2. استخدم أسماء واضحة

❌ **سيء:** page1, test, صفحة جديدة
✅ **جيد:** من-نحن, خدماتنا, اتصل-بنا

### 3. رتّب العناصر منطقياً

```
Hero Section       ← القسم الرئيسي في الأعلى
Features Grid      ← الميزات
Testimonials       ← الشهادات
Stats              ← الإحصائيات
CTA                ← دعوة لاتخاذ إجراء في النهاية
```

### 4. اختبر قبل النشر

استخدم زر **"معاينة"** (إذا متوفر) قبل النشر النهائي.

---

## 🆘 الدعم

### إذا واجهت مشكلة:

1. افتح Console (F12)
2. ابحث عن الأخطاء (باللون الأحمر)
3. اقرأ قسم "حل المشاكل" أعلاه
4. راجع ملف `COMPLETE-SYSTEM-GUIDE.md` للمزيد

### أدلة إضافية:

- `COMPLETE-SYSTEM-GUIDE.md` - دليل شامل مفصّل
- `REAL-PUBLISH-GUIDE.md` - دليل نظام النشر
- `FIXES-GUIDE.md` - المشاكل والحلول

---

## 🎉 خلاصة

### النظام الآن:

- ✅ جميع الكومبوننتس (العناصر) تعمل
- ✅ إضافة/تعديل/حفظ الصفحات يعمل
- ✅ Page Builder يفتح تلقائياً
- ✅ النشر يعمل على الخادم
- ✅ الصفحات متاحة فوراً (لا 404)
- ✅ سكريبت `START.bat` للتشغيل السهل

**كل شيء جاهز! استمتع بالعمل 🚀**

---

تم إنشاء هذا الدليل: 2025-11-13
الإصدار: 3.1 (Complete Fix with START.bat)
